{% extends 'AnytvDashboardBundle::layout.html.twig' %}

{% block title %}{{ parent() }} - {{ title}}{% endblock %}
     
{% block content_header '' %}

{% block content %}
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}
    {{ parent() }}
    <h1 class="title">{{ title|upper }}</h1>
    
    <p>&nbsp;</p>
    <div>
      {% if(app.session.get('affiliate_user_keyword')) %}
        <a href="{{ path('affiliate_users_reset') }}">reset</a>
      {% endif %}
      {{ form_start(form, {'action': path('affiliate_users'), 'method': 'POST'}) }}
         {{ form_errors(form) }}
         {{ form_errors(form.affiliate_user_keyword) }}
         {{ form_widget(form.affiliate_user_keyword, {'attr': {'class': 'affiliate_user_keyword_field'}}) }}
         {{ form_widget(form.search, {'attr': {'class': 'affiliate_user_keyword_button'}}) }}
       {{ form_end(form) }}
    </div>

    <div class="symfony-blocks-welcome">
      <table border="1" cellspacing="15" cellpadding="15">
        <tr>
         <th>Id</th>
         <th>Affiliate Id</th>
         <th>Company</th>
         <th>Region</th>
         <th>Country</th>
         <th>Date Added</th>
         <th>Modified</th>
       </tr>
       {% for affiliate_user in affiliate_users %}
         <tr>
           <td>{{ affiliate_user.id }}</td>
           <td>{{ affiliate_user.affiliate_userId }}</td>
           <td>{{ affiliate_user.company }}</td>
           <td>{{ affiliate_user.region }}</td>
           <td>{{ affiliate_user.country }}</td>
           <td>{{ affiliate_user.getDateAddedAsString() }}</td>
           <td>{{ affiliate_user.modified }}</td>
         </tr>
       {% endfor %} 
      </table>
       
      {% if (total_pages > 1) %}
        <ul>
        {% for i in 1..total_pages %}
          {% if(page == i) %}
            <li>{{ i }}</li>
          {% else %}
            <li><a href="{{ path('affiliate_users', {'page':i}) }}" title="{{ i }}">{{ i }}</a></li>
          {% endif %}
        {% endfor %}
        </ul>
      {% endif %}
        
    </div>

    <div class="symfony-blocks-help">
        
       
    </div>
{% endblock %}
