<div id="pane1" class="tab-pane active">
  <div id="user-content-offers">
    
      {% if(false) %}
      {{ form_start(form, {'action': path('profile_top_affiliates'), 'method': 'POST' }) }}
      <div class="form-group">
        <label>
          {{ form_label(form.order_by) }}
          {{ form_widget(form.order_by, {'attr': { 'onchange':"submitForm(this.form, this.form.action)" }}) }}
        </label>
      </div>
    {{ form_end(form) }}
      {% endif %}
      
      
    <div class="search-btns">				
      
        
      
	  
	
         <div class="rfrsh-wrap">
     
            
    </div>
      														
    </div>
    
    
    
          
    
  </div>
  <div class="cl-20"></div>
  <div class="progress progress-striped active" id="profile-tabs-progress-bar" style="display:none">
    <div class="progress-bar"  role="progressbar" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100" style="width: 100%"></div>
  </div>												
  <div class="table-responsive" id="profile-tab-results">
    <div class="offer-tbl-wrap">
      {% if(affiliates) %}
      <table class="table table-striped">							
        <thead>
          <tr>
            <th>&nbsp;</th>
            <th>{% trans %}Affiliate{% endtrans %}</th>
            <th>{% trans %}Clicks{% endtrans %}</th>
          </tr>
	</thead>								
        <tbody>
          {% for key, affiliate in affiliates %}
	  <tr>
            <td>{{ (key+1+offset) }}</td>
            <td>
              {% if(affiliate.thumbnail) %}
                <img src="{{ asset(affiliate.getWebPath()) | apply_filter('profile_thumbnail') }}" alt="{{ affiliate.company }}" />
              {%  else %}
                <img src="{{ asset('bundles/anytvdashboard/images/default-user-photo.png') }}">
              {% endif %}
              {{ affiliate|raw }}
            </td>
            <td>{{ affiliate.clicks }}</td>
	  </tr>
          {% endfor %}
          {% if (total_pages > 1) %}
            <tfoot>
            <tr>
	    <td class="al-cntr" colspan="6">
	    <ul class="pagination">
            {% if(page > 1) %}
              <li><a href="javascript:void(0)" onclick="showTab('{{ path('profile_top_affiliates', {'page':(page-1)}) }}');" title="{{ (page-1) }}">&laquo;</a></li>
            {% endif %}
            {% for i in 1..total_pages if((page-i)|abs < 6) %}
              {% if(page == i) %}
                <li class="active"><span>{{ i }}<span class="sr-only">(current)</span></span></li>
              {% else %}
                <li><a href="javascript:void(0)" onclick="showTab('{{ path('profile_top_affiliates', {'page':i}) }}');" title="{{ i }}">{{ i }}</a></li>
              {% endif %}
            {% endfor %}
            {% if(page < total_pages) %}
              <li><a href="javascript:void(0)" onclick="showTab('{{ path('profile_top_affiliates', {'page':(page+1)}) }}');" title="{{ (page+1) }}">&raquo;</a></li>
            {% endif %}
            </ul>
            </ul>
            </td>                                               
            </tr>
            </tfoot>    
          {% endif %}
	</tbody>
      </table>
      {% endif %}
    </div>
  </div>				    	
</div>






			
			
		
		

		
		
						
					