{% extends 'AnytvMainBundle::layout.html.twig' %}

{% block title %}{{ parent() }} - {{ title}}{% endblock %}
     
{% block content_header '' %}

{% block content %}
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}
    {{ parent() }}
    <h1 class="title">{{ title|upper }}</h1>
    
    <p>&nbsp;</p>

    <div class="symfony-blocks-welcome">
      <table border="1" cellspacing="15" cellpadding="15">
        <tr>
         <th>ID</th>
         <th>{% trans %}Name{% endtrans %}</th>
         <th>{% trans %}Description{% endtrans %}</th>
       </tr>
       {% for news_category_item in news_categories %}
         <tr>
           <td>{{ news_category_item.id }}</td>
           <td><a href="{{ path('news_category_edit', { 'id' : news_category_item.id }) }}" title="{% trans %}Edit{% endtrans %}">{{ news_category_item.name }}</a></td>
           <td>{{ news_category_item.description }}</td>
         </tr>
       {% endfor %} 
      </table>
       
       
        
    </div>

    <div class="symfony-blocks-help">
       
        <a href="{{ path('news_category_add') }}" title="{% trans %}Add{% endtrans %}">{% trans %}Add{% endtrans %}</a>
       
    </div>
{% endblock %}
